### description : client_web - targeting the browser with angularjs
the web client is designed to deliver data the application for web.

rm -rf ./node_modules
rm -rf ./dist

mkdir -p dist

npm i && npm start
